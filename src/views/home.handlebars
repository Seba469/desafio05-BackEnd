<div class="homeContainer">
    <h1>Productos</h1>
    {{#each productsList}}
        <div class="homeContainerProd">
            <h2>{{this.title}}</h2>
            <p>{{this.description}}</p>
            <p>{{this.price}}</p>
        </div>
    {{/each}}
</div>

